# Automated-Visit-counter
The entry and exit IR sensors continuously monitor movement. When an entry is detected, the microcontroller increments the visitor count; when an exit is detected, it decrements the count. The updated count is displayed on the LCD, while the buzzer provides an audio indication of the detected movement.
